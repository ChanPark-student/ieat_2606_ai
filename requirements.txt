# 기본 의존성 (LLM 없이 서버 실행 가능)
fastapi
uvicorn
pydantic
pydantic-settings
python-dotenv
# rank-bm25: 국내 리콜 대표 사례 BM25 정렬 + RAG 근거 chunk 검색에 사용 (경량, CPU 전용)
#            미설치 시 retriever만 비활성화되고 서버는 정상 동작합니다.
rank-bm25

# ──────────────────────────────────────────────────────────────────────────────
# LLM 선택 의존성 (ENABLE_LLM=true 사용 시 아래 주석을 해제 후 설치)
# 주의: torch는 용량이 크므로(~2GB+) 기본 설치에 포함하지 않습니다.
#       LLM 없이 서버를 실행하면 템플릿 기반 보고서가 자동으로 사용됩니다.
#       transformers는 5.x에서 일부 모델 호환성 문제가 있어 4.x로 상한을 둡니다.
# ──────────────────────────────────────────────────────────────────────────────
# transformers>=4.45.0,<5.0.0
# accelerate>=0.26.0
# torch>=2.1.0
